route:
  group_by: ['alertname']
  group_wait: 10s
  group_interval: 10s
  repeat_interval: 1h
  receiver: 'agent-webhook'

receivers:
- name: 'agent-webhook'
  webhook_configs:
  - url: 'http://agent:8000/webhook'
    send_resolved: true
